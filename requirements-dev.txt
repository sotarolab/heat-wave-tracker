# Test-only dependencies. Deliberately not `-r requirements.txt` - the
# tested modules (src/heat/compute.py, bias.py, extremes.py,
# historical.py) only need numpy/pandas/scipy/requests between them, so
# CI doesn't need to install the heavy GFS-fetching stack (cfgrib/
# eccodes/herbie-data) just to run them.
numpy
pandas<3.0
scipy
requests
pytest>=8.0
